#include <stdio.h>
int main()
{
    // Zero or Non Zero
    // int num = 0;
    // scanf("%d", &num);
    // if (num == 0)
    // {
    //     printf("Zero");
    // }
    // else
    // {
    //     printf("Non Zero");
    // }

       // 1. Write a c program to print 8’s time table to 200
    // for (int i = 1; i * 8 <= 200; i++)
    // {

    //     printf("8 * %d = %d\n", i, 8 * i);
    // }

    /*
    Write the differences between break and continue
    Ans: break is immidiatly end the Loop. on the otherhand,  coutinue is skip current itaration and continue with the next itaration
    */
}
