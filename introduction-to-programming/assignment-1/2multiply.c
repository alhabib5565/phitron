#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int a;
    long long int b;
    scanf("%d %lld", &a, &b);

    printf("%lld", a * b);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
